services:
  - type: web
    name: robotics-chat-backend
    env: node
    rootDir: api
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
